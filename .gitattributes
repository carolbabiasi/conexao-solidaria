# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts precisam de LF mesmo sendo editados no Windows. Com CRLF o
# shebang vira "#!/usr/bin/env bash\r" e o Linux responde
# "bad interpreter: No such file or directory".
*.sh text eol=lf
